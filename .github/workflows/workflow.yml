name: build

on: push

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        linux: [gentoo, linux-mint, ubuntu,
          almalinux, amazonlinux, centos, centos8, fedora,
          mageia, opensuse, oraclelinux, rockylinux,
          archlinux, chimeralinux,
          ] # redhat, chimeralinux, debian, kalilinux, void-linux
    uses: takano32/build-kernel/.github/workflows/build.yml@master
    with:
      linux: ${{ matrix.linux }}

