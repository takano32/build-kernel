name: build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3
      - name: Build docker images
        run: docker build -t build-kernel .
      - name: Run tests
        run: docker run -it -v $PWD:/srv -w/srv build-kernel

